---
import type { Recipe } from '@data/recipe';
const recipe: Recipe = Astro.props.recipe
---

<!-- <div class="flex items-start">
    <div class="flex flex-col border-b border-dashed border-base-300 pl-4">
        <h2 class="text-sm lg:text-lg font-bold">
            <a class="hover:underline" href={`/${recipe.slug}`}>{recipe.title}</a>
        </h2>
        <p class="text-sm text-base-content/70">{recipe.desc}</p>
    </div>
</div> -->

<div class="card w-96 bg-base-300 shadow-xl">
    <figure class="p-0">
        {recipe.img && (
            <a href={`/${recipe.slug}`} class="shrink-1">
              <img
                src={recipe.img.toString()}
                alt={recipe.title} // Using alt from Post interfac
              />
            </a>
          )}
        <!-- <img src="https://daisyui.com/images/stock/photo-1606107557195-0e29a4b5b4aa.jpg" alt="Shoes" /> -->
    </figure>
    <div class="card-body">
      <h2 class="card-title">{recipe.title}</h2>
      <!-- <a href={`/${recipe.slug}`}>{recipe.desc}</a> -->
      <!-- <div class="card-actions justify-end">
        <button class="btn btn-primary">Buy Now</button>
      </div> -->
    </div>
  </div>
 